import publications from './publications';

const fixture = [
  { publicationId: publications[0].id, url: 'https://pub.com/feed' },
  { publicationId: publications[1].id, url: 'https://tem.com/feed' },
];

export default fixture;
