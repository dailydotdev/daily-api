export enum ContentPreferenceType {
  User = 'user',
  Keyword = 'keyword',
  Source = 'source',
}

export enum ContentPreferenceStatus {
  Follow = 'follow',
  Subscribed = 'subscribed',
  Blocked = 'blocked',
}
