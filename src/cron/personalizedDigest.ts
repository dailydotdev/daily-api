import { Cron } from './cron';

const cron: Cron = {
  name: 'personalized-digest',
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  handler: async (con, logger) => {
    // TODO WT-1820-personalized-digest implement and remove lint disable
  },
};

export default cron;
